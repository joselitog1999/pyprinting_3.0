# Architecture & Experimental Decision Log — PyPrinting 3.0

**Location**: `docs/decisions/DECISION_LOG.md`  
**Purpose**: Chronological log of major architectural, experimental, and calibration choices made in PyPrinting 3.0.

---

## Decisions Index

| Decision ID | Date | Title | Status | Deciders | Linked Docs |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `DEC-001` | 2026-08-10 | Decoupling Power Flipper from Shutter Safety Lines | `ACCEPTED` | Instrumentation, QA | `SYS-202` |
| `DEC-002` | 2026-08-20 | Dedicated 500 ms Autonomous Watchdog Heartbeat | `ACCEPTED` | Software Architect, HAL | `SYS-201` |
| `DEC-003` | 2026-09-01 | Adoption of HDF5 Container with Shuffle+Gzip | `ACCEPTED` | Metrology, Data Scientist | `CAT-401` |
| `DEC-004` | 2026-09-16 | Migration to Claude Lab OS (Agents + Skills Ecosystem) | `ACCEPTED` | Lead Dev, Meta-Architect | `CLAUDE.md` |
| `DEC-005` | 2026-09-16 | Event Isolation via `_results_stale` Flag in Lattice Disorder Analyzer (Bloque 5/6/Fourier) | `ACCEPTED` | Software Architect, QA/UX, Metrology | `SW-001`, `analysis/lattice_disorder_gui.py` |
| `DEC-006` | 2026-09-16 | Real `QThread` (`moveToThread`) for LineScanSpectroscopyWorker — First Exception to the QTimer-on-GUI-Thread Convention in PySpectrum | `ACCEPTED` | Software Architect, Instrumentation | `pyspectrum/modules/routines/linescan_spectroscopy.py` |
| `DEC-007` | 2026-09-16 | Formal ADR: Dedicated `QThread` Worker for LineScan Spectroscopy (Considered Options & Traceability) | `ACCEPTED` | Software Architect, Instrumentation | `SYS-101`, `SYS-102`, `SYS-104`, complements `DEC-006` |
| `DEC-008` | 2026-09-16 | Metrological Traceability of Transmittance/Extinction Datasets in LineScan Spectroscopy HDF5 (Raw vs. `_physical`) | `ACCEPTED — FULLY RESOLVED (2026-09-17)` | Scientific Reviewer, Metrology | `SYS-104`, `CAT-401`, `core/sif_processor.py` |
| `DEC-009` | 2026-09-17 | Hardware-Safety Audit Remediation: Slit Clamp, Grating Settle Polling, Session Arbitration, Partial-Scan Reporting (P0) | `ACCEPTED` | Instrumentation, Software Architect, QA/UX Auditor, Metrology | `pyspectrum/drivers/shamrock_driver.py`, `pyspectrum/modules/step_and_glue.py`, `pyspectrum/modules/routines/linescan_spectroscopy.py` |
| `DEC-010` | 2026-09-17 | Unified Global Shutter Auto-Close Policy (SYS-201) + Heartbeat Coverage Audit Across Scanning Loops | `ACCEPTED` | Instrumentation, Software Architect | `core/nidaq.py`, `core/shutters.py`, `modules/confocal.py`, `modules/focus.py`, `contrapropagante.py` |
| `DEC-011` | 2026-09-17 | PI E-517 Stage Connection Resilience: GCSError/Communication-Failure Isolation, MOV Clamping, Grid Pre-Flight Range Check, Pause/Resume on Real Disconnection | `ACCEPTED` | Instrumentation, Software Architect | `config.py`, `modules/measurements.py` |
| `DEC-012` | 2026-09-18 | Universal Template Matching and Crystallographic Generalization for Hexagonal and Honeycomb Lattices in 2D Disorder Analyzer | `ACCEPTED` | Physicist, Computational Physicist, Metrology, Software Architect, Devil's Advocate | `core/lattice_disorder.py`, `analysis/lattice_disorder_gui.py`, `core/lattice_generator.py` (consumed, not modified), `MOD-08` |
| `DEC-013` | 2026-09-18 | Multi-Agent Hardware-Safety Audit Remediation (Lotes 1-6): Focus Retry Caps, Trace Task Persistence, Printing Watchdog Renewal, Confocal Settling & Heartbeat Policy, Hyperspectral QThread + Shutter Lifecycle, Dual-Confocal PSF-Mode Parity | `ACCEPTED` | Instrumentation, Software Architect, Experimentalist, Devil's Advocate | `modules/focus.py`, `modules/trace.py`, `core/nidaq.py`, `modules/measurements.py`, `modules/confocal.py`, `contrapropagante.py`, `pyspectrum/modules/hyperspectral_confocal.py`, `pyspectrum/window.py` |
| `DEC-014` | 2026-09-19 | Canon EDSDK Camera Stability Audit + `StageCameraTransform` Kinematics + Bidirectional Spatial Sync (Cámara ↔ Confocal ↔ Impresión) | `ACCEPTED` | Software Architect, Instrumentation, QA/UX Auditor, Metrology, Devil's Advocate | `core/canon_edsdk.py`, `modules/camera.py`, `app.py`, `core/nanopositioning.py`, `core/stage_camera_transform.py`, `core/stage_camera_calibration.py`, `MOD-04` |

---

## Summary of Key Historic Decisions

### DEC-001: Decoupling Power Flipper from Shutter Safety Lines
* **Context**: Previously, moving the neutral density (ND) optical flipper relied on shared digital lines, causing race conditions when emergency shutters triggered.
* **Decision**: Actuate the flipper strictly via analog voltage steps (+5V) on `Dev1/ao0` and `Dev1/ao1`, completely isolated from digital lines `port0/line0:3`.
* **Outcome**: Shutter panic close is unconditionally preserved regardless of flipper motion state.

### DEC-006: Real `QThread` for LineScanSpectroscopyWorker
* **Context**: Every existing PySpectrum routine (`growth_kinetics.py`, `hyperspectral_confocal.py`, `dimers.py`, `luminescence.py`) runs its `Backend(QObject)` on the GUI thread, driven by a `QTimer` whose steps are short (~20ms) and non-blocking. The new linescan routine's steps can legitimately block from hundreds of ms (single-window mode) to several seconds/minutes per spatial point (Step & Glue mode: multiple grating moves, each gated by `wait_until_ready()`, plus exposure time). A software-architect + instrumentation review of the blueprint found that running this in the GUI thread would risk the shutter heartbeat and the emergency-abort button going unresponsive during a blocking `time.sleep()`/polling call.
* **Decision**: `LineScanSpectroscopyWorker(QObject)` is moved to a dedicated `QThread` via `moveToThread()` (see `create_linescan_routine()` in `pyspectrum/modules/routines/linescan_spectroscopy.py`). The worker never relies on `hardware_session.emergencyStopSignal` to interrupt an in-progress blocking wait (a Qt signal is only delivered when the receiving thread returns to its event loop); instead it polls the thread-safe `hardware_session.is_emergency_stopped` property between every blocking sub-step, mirroring the `_abort_requested` pattern already used in `step_and_glue.py`. `heartbeat_shutter(30.0)` is called on every tick of any blocking wait (piezo settle polling, grating settle polling, exposure wait), not just once per step, since a single Step & Glue point can exceed the watchdog's 30s timeout.
* **Outcome**: First routine in PySpectrum 3.0 to use a real `QThread`. GUI remains responsive and the local/global abort paths remain effective during arbitrarily long acquisitions. Verified via `tests/test_linescan_h5.py` (9/9 passing) and the full `pytest tests/` suite (176 passed, no regressions).

### DEC-007: Formal ADR — Dedicated `QThread` Worker for the Linear Scan Spectroscopy Routine

* **Decision ID**: `DEC-007`
* **Title**: Dedicated `QThread` (`moveToThread`) Worker for the Linear Scan Spectroscopy Routine — First Departure from PySpectrum's QTimer-on-GUI-Thread Convention
* **Date**: 2026-09-16
* **Status**: `ACCEPTED`
* **Deciders**: Software Architect, Instrumentation

**1. Context & Problem Statement**
PySpectrum 3.0 has five acquisition routines. Four of them — `growth_kinetics.py`, `hyperspectral_confocal.py`, `dimers.py`, `luminescence.py` — run a `Backend(QObject)` on the GUI thread, driven by a `QTimer` whose steps are short (~20 ms) and non-blocking. The new Linear Scan Spectroscopy routine (`linescan_spectroscopy.py`) breaks that assumption: a single spatial point can block for hundreds of ms (Single-Window mode) to several seconds or minutes (Step & Glue mode), stacking piezo settle waits, one or more grating moves gated by `spectrometer.is_moving()`, and camera exposure time. Running this on the GUI thread would starve the Qt event loop during each blocking call: neither the shutter watchdog heartbeat nor the emergency-abort button could be serviced, since `pyqtSignal` delivery — including `hardware_session.emergencyStopSignal` — only happens once the receiving thread returns to its event loop.

**2. Decision Drivers**
* Shutter watchdog heartbeat and emergency-abort must stay serviceable through acquisitions that can exceed the watchdog's 30 s heartbeat timeout by orders of magnitude.
* GUI responsiveness (`SYS-101`) must hold under sustained blocking hardware polling.
* Reuse already-validated abort and HDF5 patterns over new abstractions.
* Preserve Liskov substitution on the HDF5 container schema.

**3. Considered Options**
* **Option A — Keep the `QTimer`-on-GUI-thread convention**: consistent with the other four routines; rejected because a single blocking sub-step (grating settle, exposure) can exceed the heartbeat cadence by orders of magnitude, leaving shutters and abort unresponsive.
* **Option B — `AcquisitionStrategy` class hierarchy** for the two acquisition modes: rejected as over-engineering; `step_and_glue.py` already resolves the same two-mode dilemma with sibling methods and no base class.
* **Option C (chosen) — `LineScanSpectroscopyWorker(QObject)` on a dedicated `QThread`** via `moveToThread()`, polling `hardware_session.is_emergency_stopped` between every blocking sub-step instead of trusting signal delivery mid-wait, mirroring the `_abort_requested` pattern already used in `step_and_glue.py`.

**4. Decision Outcome**
Chosen option: **Option C**, the only one that keeps the watchdog and abort paths responsive through arbitrarily long blocking waits without adding unneeded abstractions. `create_linescan_routine()` builds `LineScanSpectroscopyWidget` + `LineScanSpectroscopyWorker`, calls `worker.moveToThread(thread)`, wires widget→worker request signals and worker→widget result signals, then starts the thread. `_should_abort()` (`_cancel_requested or hardware_session.is_emergency_stopped`) gates `_sleep_with_heartbeat()`, `_move_and_settle()` (piezo, `pi.qONT()` polling, 5 s timeout) and `_settle_wavelength()` (grating, `spectrometer.is_moving()`, 6 s timeout); each re-issues `heartbeat_shutter(30.0)` per tick, not just once per step. Exposure wait adds `ACQUISITION_READOUT_MARGIN_S` (0.05 s, a documented placeholder — no real EMCCD readout model exists yet). Persistence uses the standalone `write_linescan_spectroscopy_hdf5()`, not a `BatchHDF5Container` subclass, reusing only its `shuffle+gzip(4)` compression under an independent hierarchical schema.

*Positive Consequences*: first PySpectrum routine with a verified-responsive abort/watchdog path across arbitrarily long acquisitions; no blind `time.sleep()` — every settle wait is hardware-polled with an explicit timeout and post-settle verification (`pi.qPOS` vs. `PIEZO_POSITION_TOLERANCE_UM`); zero LSP violation on the HDF5 layer.

*Negative Consequences / Trade-offs*: introduces PySpectrum's first real `moveToThread()` lifecycle, requiring explicit teardown (`_cleanup()` in `create_linescan_routine()`, plus `PySpectrumWindow.closeEvent()`) that the `QTimer` routines never needed; `ACQUISITION_READOUT_MARGIN_S = 0.05 s` remains an undocumented-physics placeholder pending a real EMCCD readout model; the two acquisition modes diverge structurally (sibling methods, no shared interface) — the same shape already used in `step_and_glue.py`.

**5. Physical / Code Traceability**
* **Affected Modules**: `pyspectrum/modules/routines/linescan_spectroscopy.py` (`LineScanSpectroscopyWorker`, `LineScanSpectroscopyWidget`, `create_linescan_routine`), `core/hdf5_container.py` (`write_linescan_spectroscopy_hdf5`), `tests/test_linescan_h5.py`.
* **Linked Compendiums**: `SYS-101`, `SYS-102`, `SYS-104`; complements `DEC-006`.

### DEC-008: Metrological Traceability of Transmittance/Extinction Datasets in LineScan Spectroscopy
* **Context**: Peer review of `linescan_spectroscopy.py`'s T/E pipeline and its HDF5 serialization (`core/hdf5_container.py::write_linescan_spectroscopy_hdf5`), specifically whether reusing `sif_processor.py`'s shared functions and storing an unclamped raw dataset alongside a derived `_physical` one is metrologically defensible.
* **Decision / Review Verdict**: Reusing `compute_transmittance_with_errors()`/`compute_extinction()` (shared with `sif_analyzer.py`) is the correct traceability choice — both modules derive T/E from the same audited implementation rather than parallel reimplementations. The raw-vs-physical split is metrologically sound: clamping raw T would bias a symmetric dark-noise residual (a GUM violation), so an unclamped raw dataset plus a derived `transmission_*_physical` copy is consistent practice, and the NaN gate (weak reference) is properly decoupled from the positivity clamp (signal sign) — two orthogonal guards, not conflated.
  Two non-blocking gaps were found during the initial review: (1) `transmission_*_physical = clip(T, 0, None)` floored at exactly `0.0`, not the `1e-6` epsilon `compute_extinction()` uses internally — a non-Python consumer computing `-log10()` directly on `_physical`, as its own `rationale` attribute invited, would hit `+inf` rather than a finite value. Note that `extinction_1d/2d` in this pipeline are still computed from the raw `T`, not from `_physical` — the derived dataset is written for external/downstream consumers but is not itself consumed anywhere in this pipeline; this remains true after the fix below and is not itself a defect (the raw dataset is metrologically the correct source for extinction). (2) The noise multiplier and per-row `noise_threshold_2d` were not persisted to HDF5, only inferable from `noise_threshold_1d`/`sigma_dark_1d`.
* **Outcome**: Core design **ACCEPTED — FULLY RESOLVED (2026-09-17)**. Both gaps closed:
  1. `EPSILON_TRANS = 1e-6` (`linescan_spectroscopy.py`) now floors `transmission_1d_physical`/`transmission_2d_physical` in `run_scan()`, matching `compute_extinction()`'s internal epsilon exactly. `write_linescan_spectroscopy_hdf5()` stamps `clipping_floor = 1e-6` alongside `clipping_applied = True` and an updated `rationale` attribute stating the epsilon explicitly and clarifying that rigorous metrological calculations must consume the raw `transmission_*` dataset, not `_physical`.
  2. `LineScanSpectroscopyWorker.acquire_reference()` now stores `noise_threshold_2d` (per-row, `mult * sigma_dark_2d`) in `self._reference` and tracks `self.noise_multiplier`; `write_linescan_spectroscopy_hdf5()` persists it as `/reference/noise_threshold_2d` (shape `[N_roi_y]`); `run_scan()` writes `noise_multiplier` (float, e.g. `3.0`) into `/metadata` attrs.
  Verified against `tests/test_linescan_h5.py` (10/10 passing — `test_transmission_physical_never_negative` updated to assert the `1e-6` floor and `clipping_floor` attribute; new `test_noise_threshold_2d_and_multiplier_persisted` asserts presence/shape of `/reference/noise_threshold_2d` and the `/metadata` `noise_multiplier` attribute). Full suite re-run before and after the fix confirms 0 regressions attributable to this change (`tests/` full collection: 177 passed both times when isolated from a pre-existing, unrelated set of 7 failures/2 errors caused by a known environment-level `PyQt6` DLL loading issue in `test_hardware_dashboard_logic.py`, `test_pyspectrum_stability_safety.py`, and `test_shutter_alignment_and_heartbeat.py` — reproduced identically with and without this change via `git stash`, confirming no causal link).

### DEC-009: Hardware-Safety Audit Remediation (P0) — Slit Clamp, Grating Settle Polling, Session Arbitration, Partial-Scan Reporting
* **Context**: A full 5-phase hardware-readiness audit of `pyspectrum/` (panels: `instrumentation`, `software-architect`, `qa-ux-auditor`, `metrology`) evaluating readiness to connect to the real Andor Shamrock SR-500i / iXon3 EMCCD / PI E-517 bench, prior to `DEC-009`. The audit returned a 🔴 `PELIGRO_DE_HARDWARE_BLOQUEADO` verdict on four independent P0 findings, detailed below, plus a root-caused test-hygiene bug that was masking the audit's own verification runs.
* **Decision — 4 patches applied, diagnosis-only findings NOT applied (logged as P1/P2 follow-ups)**:
  1. **P0-A (`pyspectrum/drivers/shamrock_driver.py::ShamrockDriver.set_slit()`)**: the real driver passed slit width straight to `self._dll.ShamrockSetSlit()` with **no clamping** — only `_MockShamrock` clamped to `10.0–2500.0 µm`, a mock/real parity violation and a hardware-safety-gate violation (CLAUDE.md §4: physical interlocks must never be bypassable). Fixed by clamping identically in the real driver before the DLL call, so an operator entering an out-of-range slit width in `spectrum_control.py`/`calibration_dock.py` can no longer drive the motorized blades against their mechanical hard-stop.
  2. **P0-B (`pyspectrum/modules/step_and_glue.py`, both `ShamrockSetWavelength()` call sites)**: used a hardcoded `time.sleep(0.05)` instead of confirming grating settle — 6× under the driver's own `WAVELENGTH_SETTLING_TIME_S = 0.3 s` constant. Every point of a Step & Glue panoramic scan was captured while the wavelength micrometer could still be moving, a systematic and silent spectral-smearing artifact. Fixed by adding `Backend._settle_wavelength()`, a direct port of `linescan_spectroscopy.py::LineScanSpectroscopyWorker._settle_wavelength()` (`is_moving()` polling, `heartbeat_shutter(30.0)` renewed per tick, bounded by a 6 s timeout, abort-aware via `self._abort_requested`), replacing both sleep sites.
  3. **P0-C (`pyspectrum/modules/step_and_glue.py::Backend.measure_single_spectrum()`)**: never called `hardware_session.acquire_session()` — only `measure_step_and_glue()` did. A "Single Spectrum" click could run concurrently with any other session-holding routine, racing on the shared Shamrock/Andor driver handles that `hardware_session` exists to arbitrate. Fixed by wrapping the method body in `acquire_session("Step & Glue — Espectro Único")` / `release_session(...)` (`try/finally`), matching the pattern already used in `measure_step_and_glue()`.
  4. **P0-D (`pyspectrum/modules/routines/linescan_spectroscopy.py::run_scan()`)**: if `_move_and_settle()`/`_settle_wavelength()` hit their own hardware timeout mid-scan (as opposed to a user cancel or E-STOP), the step loop `break`s but neither `_cancel_requested` nor `is_emergency_stopped` becomes `True` — so execution fell through the post-loop guard, wrote the (now partially-`NaN`) HDF5, and emitted `scanCompletedSignal` → a genuine "Escaneo Finalizado" success dialog for a corrupt dataset. Fixed with a new `self._scan_failed` flag, set **only** in the genuine-timeout branch of `_move_and_settle()`/`_settle_wavelength()` (never in their `_should_abort()` branch, which is already correctly handled by the existing cancel/E-STOP guard). `run_scan()` now stamps `metadata['scan_aborted']` and, when the flag is set, emits a new `scanAbortedSignal(h5_path, reason)` instead of `scanCompletedSignal` — the widget's new `on_scan_aborted()` slot shows a distinct "Escaneo Incompleto — Fallo Mecánico" warning, never the success dialog. The partial HDF5 is still written (not discarded) for forensic recovery of the valid points preceding the failure.
  5. **Test-hygiene root cause (not a production defect)**: the audit traced 5 of `tests/test_pyspectrum_stability_safety.py`'s own failures to `TestPySpectrumStability::test_window_lifecycle_and_clean_exit`, which legitimately triggers `hardware_session.emergency_stop()` via `PySpectrumWindow.closeEvent()` but never reset it — since `hardware_session` is a process-wide singleton, the E-STOP state leaked into every subsequent test (`TestPySpectrumLegacyRoutines` included), rejecting every `acquire_session()` call with "E-STOP ACTIVO" for the rest of the process. Fixed by adding `tearDown(self): hardware_session.clear_emergency()` to `TestPySpectrumStability`.
* **Deferred (P1/P2, diagnosis-only, not applied in this pass)**: Peltier temperature lock-in polling before first acquisition; explicit `camera.abort_acquisition()` in routine-level exception/`finally` handlers; propagating/checking the `ret` code on `ShamrockGetCalibration()` instead of silently falling back to a fabricated `linspace(400,700)` axis (`static_raman.py`, `hyperspectral_confocal.py`); persistent (non-transient) E-STOP banner and unifying `closeEvent()`'s UI feedback path with the E-STOP button's; 4 orphaned `pyqtSignal`s (`pointSelectedSignal`, `scanFinishedSignal`, `requestCalibrationSignal`, `copyClipboardSignal`); `window.py::closeEvent()` calling `linescan_worker.cancel_scan()` directly instead of via signal; missing lowercase pythonic aliases on `_MockShamrock`; adopting the cubic EEPROM calibration path in `hyperspectral_confocal.py`/`luminescence.py`/`growth_kinetics.py`/`dimers.py`; provenance metadata (calibration source, exposure, overlap) in Step & Glue's `.txt`/`.npz` exports.
* **Outcome**: **ACCEPTED**. `tests/` full collection: 182 passed (up from 177), 2 failed + 2 errors remaining — confirmed via two consecutive full-suite runs to be the same pre-existing, environment-level `PyQt6` DLL-loading artifacts already characterized in `DEC-008` (`test_hardware_dashboard_logic.py::test_hardware_manager_isolation`, `test_pyspectrum_stability_safety.py::TestPySpectrumSafety::test_calibration_txt_persistence_roundtrip`, `test_shutter_alignment_and_heartbeat.py` collection errors), unrelated to this change. `graphify update .` run cleanly post-edit. Hardware-readiness verdict updated to **🟢 `LISTO_PARA_BANCO_OPTICO` — MODO MANUAL/ASISTIDO ÚNICAMENTE**: the four P0 findings were all either mechanical-damage risks (unclamped slit) or silent-data-corruption/collision risks (unsettled grating capture, session-arbitration bypass, false-success reporting on partial scans), and are now closed. This 🟢 covers an operator actively supervising the bench (present to notice a failed cooling LED, a status-bar rejection, or an unexpected dialog). It explicitly does **not** cover unattended/automated multi-hour runs (e.g. an overnight Step & Glue sweep with nobody watching) — that requires the P1 items above first, specifically the Peltier lock-in gate (no cooling confirmation before acquisition today) and the E-STOP UI persistence fix (still a transient 6 s status-bar toast, easy to miss if unattended).
* **Correction (logged in `DEC-010`, 2026-09-17)**: the `test_shutter_alignment_and_heartbeat.py` "collection errors" cited above as a `PyQt6` DLL-loading artifact were misdiagnosed. The real cause was a missing pytest fixture (`app`) and, separately, a deterministic `sys.path` ordering issue (two `PyQt6` installs for Python 3.13 in this environment — see `DEC-010` for the full root cause and fix). Both are now fixed; see `DEC-010`.

### DEC-010: Unified Global Shutter Auto-Close Policy (SYS-201) + Heartbeat Coverage Audit Across Scanning Loops
* **Context**: `core/nidaq.py::open_shutter()`/`heartbeat_shutter()` hardcoded `timeout_s=30.0` as their Python default-parameter value. `core/shutters.py::Backend.set_autoclose_timeout()` (the slot behind the Shutters dock's "Auto-cierre" checkbox / timeout combo) only stored the choice in `self.current_timeout`, a per-instance attribute consulted **only** by that same dock's own `shutter0()`–`shutter3()` slots. Every other module opening a shutter — every experimental routine — calls `open_shutter(self.laser)` with no `timeout_s` argument, so it silently got the hardcoded `30.0` regardless of what the operator had selected in the dock, including "Sin límite (Modo Alineación)". Separately, several scanning loops opened a shutter once at the start of a multi-row/multi-point acquisition and never called `heartbeat_shutter()` again until the scan finished — if a single scan exceeded the (possibly silently-restored) 30 s deadline, the watchdog would force-close the shutter mid-scan, leaving the stage scanning in the dark.
* **Decision**:
  1. **Global policy, `core/nidaq.py`**: added `_default_timeout_s` (module global) + `set_default_shutter_timeout()`/`get_default_shutter_timeout()`. `open_shutter()` and `heartbeat_shutter()` now default to a sentinel (`_SENTINEL`, an `object()` identity check, not `None` — `None` is itself a valid explicit "no limit" value and must stay distinguishable from "no argument passed") and fall back to `get_default_shutter_timeout()` only when the caller passed nothing. An explicit `timeout_s` argument (numeric or `None`) at any call site always wins over the global policy, preserving every existing explicit call site's behavior unchanged.
  2. **Wiring, `core/shutters.py`**: `Backend.set_autoclose_timeout()` now also calls `set_default_shutter_timeout(timeout_val)`, so a change in the dock takes effect for every module in the process immediately, not just for that dock's own four shutter buttons.
  3. **Heartbeat audit across scanning loops** (all following the existing `heartbeat_shutter(30.0)` per-iteration pattern already established in `modules/trace.py` and `pyspectrum/modules/routines/linescan_spectroscopy.py`/`step_and_glue.py` — kept as explicit `30.0` deliberately, since these are the routine's own bounded-interval safety renewal, not a substitute for the global policy, which only governs the *initial* `open_shutter()` call):
     - `modules/confocal.py`: added to `_scan_ramp_xy/_xz/_yx/_yz()` (once per row), `_scan_step_xy()` (once per pixel), `_measure_4_corners_tilt()` (once per corner) — none of these had `heartbeat_shutter` imported at all before this change.
     - `modules/focus.py`: added to `focus_go_to_maximum()`/`focus_lock_lin()`'s `while flag:` retry loops (defense-in-depth; already indirectly covered since each retry calls `open_shutter()` again), `focus_autocorr_lin_x2()`'s `for _ in range(2):` loop (the shutter stays open across **both** iterations, previously with zero renewal in between), and `_focus_autocorr_lin()`'s own internal `while flag:` retry loop (the shared low-level primitive called by `confocal.py::_measure_4_corners_tilt()` too — fixing it once here covers all callers).
     - `contrapropagante.py`: same architecture as `confocal.py`'s ramp scan (`_scan_ramp_xy()`), same missing-heartbeat bug, same fix. Also fixed an unrelated pre-existing latent bug found in the same file while editing its import line: `close_all_shutters()` was called at two sites (`stop_scan()`, `_scan_ramp_xy()`'s completion branch) but was never imported — would have raised `NameError` the first time either path executed.
  4. **Verified NOT to need a fix** (checked directly, not assumed): `modules/measurements.py`'s `_grid_trace()`/`grid_trace_detect()` delegate the actual acquisition loop to `modules/trace.py`'s `Backend` (wired via `grid_traceSignal` → `traceWorker.trace_configuration` in `app.py`), which already renews `heartbeat_shutter(30.0)` every ~30 frames — no independent loop of its own. `pyspectrum/modules/hyperspectral_confocal.py` never calls `open_shutter`/`close_shutter`/`heartbeat_shutter` at all (camera+piezo mapping only, no shutter control). `modules/camera.py`'s only `open_shutter` call is a manual toggle button (`btn_shutter`), not a loop — it benefits automatically from fix #1/#2 above, needs no loop-level fix. `pyspectrum/modules/routines/growth_kinetics.py` and `luminescence.py` already called `heartbeat_shutter(30.0)` correctly inside their `QTimer` step handlers before this change. `pyspectrum/modules/routines/dimers.py` never touches shutters. `pyspectrum/modules/routines/linescan_spectroscopy.py`/`step_and_glue.py` were already fixed for this in `DEC-009`.
  5. **Test infrastructure fix (unplanned but required to validate #1–#4)**: while investigating `tests/test_shutter_alignment_and_heartbeat.py::test_watchdog_callback_and_ui_sync`/`test_ui_alignment_mode_selection` — previously misdiagnosed in `DEC-009` as a "PyQt6 DLL-loading environment artifact" — found the real, deterministic cause: (a) both tests take `app` as a parameter with no pytest fixture ever defining it anywhere in the repo (`fixture 'app' not found`); (b) separately, this environment has **two** `PyQt6` installations for Python 3.13 (a broken one in the conda base env, a working one in `.venv/Lib/site-packages/`), and `config.py` only prepends `.venv/Lib/site-packages` to `sys.path` once `config` itself has been imported — so any file importing `PyQt6.QtWidgets` before ever importing `config` deterministically resolves the broken conda install and crashes with `ImportError: DLL load failed while importing QtWidgets`, regardless of process history ("warm-up" was never real — it was files importing `config` transitively before `PyQt6`). Fixed with a new `tests/conftest.py` (imports `config` at module scope, before pytest collects any test file, so `sys.path` is corrected process-wide; defines the missing `app` session fixture, importing `PyQt6.QtWidgets` lazily inside the fixture body itself — importing it eagerly at `conftest.py` module scope was tried first and made it the *first* PyQt6 import of the whole pytest process, which reintroduced the exact same crash for the **entire** suite, not just these two tests) and reordering the equivalent import in `test_shutter_alignment_and_heartbeat.py`'s own `__main__`-script path (`import config` before `from PyQt6.QtWidgets import ...`), since running that file directly via `python tests/test_shutter_alignment_and_heartbeat.py` never loads `conftest.py` at all.
  6. **New regression tests**: `test_experimental_routine_respects_global_alignment_policy()` and `test_experimental_routine_respects_global_30s_policy()` added to `tests/test_shutter_alignment_and_heartbeat.py` — directly exercise the Shutters dock UI → `open_shutter(laser)` (no explicit `timeout_s`, the real signature every experimental routine uses) → assert the watchdog does/doesn't arm according to the dock's current selection.
* **Outcome**: **ACCEPTED**. `python tests/test_shutter_alignment_and_heartbeat.py` and `python tests/test_concurrency_watchdog.py` (the exact commands requested): all 6 + 2 tests pass. `pytest tests/` full collection: **186 passed** (up from 182 in `DEC-009`), **2 failed, 0 errors** (down from 2 failed + 2 errors) — the 2 previously-erroring shutter-alignment tests now pass; the 2 remaining failures (`test_hardware_dashboard_logic.py::test_hardware_manager_isolation`, `test_pyspectrum_stability_safety.py::TestPySpectrumSafety::test_calibration_txt_persistence_roundtrip`) are confirmed **not** DLL-related (direct tracebacks captured: the former is a genuine, reproducible logic bug in `core/hardware_manager.py::toggle_isolation()` unrelated to shutters — `is_isolated()` stays `True` after `toggle_isolation(dev, False)`; the latter only fails when run as part of the *entire* suite, not within its own file group, indicating separate cross-test state pollution from an as-yet-unidentified source). Both are pre-existing, outside this task's scope (shutter/watchdog policy), and logged here as open follow-up items rather than silently left mischaracterized as "environment noise." `graphify update .` run cleanly post-edit.

### DEC-011: PI E-517 Stage Connection Resilience — GCSError Isolation, MOV Clamping, Grid Pre-Flight Range Check, Pause/Resume on Real Disconnection
* **Context**: The PI E-517 piezo stage was disconnecting into irreversible "Modo Virtual" mid-unattended-experiment (overnight grid printing, long scans, drift tracking). Root cause: `config.py::_PIController.MOV()`/`qPOS()`/`qONT()` wrapped every call to `pipython.GCSDevice` in a bare `except Exception: self._connected = False`, so (a) a firmware-level command rejection (`GCSError -1004 "Position out of limits"`, triggered whenever a grid computation — `startX`/`startY` + drift-corrected `shiftx`/`shifty` — landed a fraction of a micron outside `[0, 100] µm`) and (b) a transient bus-busy read collision during an active `qONT()` polling loop were both indistinguishable, at the driver level, from the USB cable being physically unplugged. `is_physically_connected()` additionally sent `*IDN?` over the USB bus on every position-refresh poll, adding bus contention during active motion. None of this happened in the legacy driver, which never tore down the connection on a command-level error.
* **Decision**:
  1. **`config.py::_PIController.MOV()` (and `_MockPI.MOV()`, for testability — `pipython` isn't installed in this dev/test environment, so `pi` is always `_MockPI` here)**: unconditional clamp to `[0.0, PI_STAGE_RANGE_UM]` before the coordinate ever reaches the device, logging `[PI Driver Clamped]` when a value was actually out of range. This is the single point that eradicates `GCSError -1004` at the source, regardless of whether any individual caller (`modules/confocal.py`, `modules/measurements.py`, etc.) remembered to clamp itself.
  2. **`GCSError` isolation**: `MOV()`/`qPOS()`/`qONT()` now catch `GCSError` (imported from `pipython`, with a local fallback `class GCSError(Exception)` when `pipython` isn't installed, so the `except GCSError:` clause is always syntactically valid) as its own branch that logs and **never** touches `self._connected`. Only a **non**-`GCSError` exception — a genuine low-level communication failure — can still lead to a disconnection.
  3. **Retries, `qPOS()`/`qONT()`**: new `_retry_read()` helper retries a failed read up to 2 times (20 ms apart) before giving up. On persistent failure, `qPOS()` returns the last known cached position (`self._pos`) and `qONT()` assumes on-target — **neither ever sets `self._connected = False`**, per explicit design: a read hiccup during active motion (the documented collision scenario) is not evidence of a dead connection.
  4. **`is_physically_connected()`**: no longer sends `*IDN?` on every call. Uses `GCSDevice.IsConnected()` (a host-side state query with no USB traffic) when available, falling back to the in-memory `self._connected` flag otherwise.
  5. **`try_auto_reconnect()`**: new method, called from `MOV()`'s non-`GCSError` exception branch — one transparent `connect()` attempt before `self._connected` is finally set to `False`. Only a `MOV()` failure that is both (a) not a `GCSError` and (b) survives a failed auto-reconnect attempt now results in a real disconnection — the narrowest possible trigger, matching "solo error irrecuperable de comunicación" from the mission brief.
  6. **Grid pre-flight range check, `modules/measurements.py::Backend`**: new `_preflight_grid_range_check()`, called at the top of `_grid_start()` (before any `pi.MOV()` or `open_shutter()`), computes the grid's total bounding box (`startX`/`startY` ± `grid_x`/`grid_y` extent, plus a 3 µm thermal-drift margin) and aborts — emitting the new `gridRangeErrorSignal(str)` → `Frontend._show_grid_range_error()` (a `QMessageBox.warning`) — if it exceeds `[0, PI_STAGE_RANGE_UM]`. This catches a scientifically-distorted grid (edge particles silently clamped to a different position than calculated) *before* any laser opens, which the driver-level clamp in #1 alone cannot do (it protects the hardware, not the experiment's geometric validity). Also added an explicit redundant clamp at `target_insitu_x`/`target_insitu_y` (Stage 3/4, in-situ autofocus) per the mission brief, defense-in-depth alongside #1.
  7. **Pause/resume state machine on real disconnection**: new `Backend._check_physical_connection_or_pause()`, called at the top of `_grid_move()` (the single choke point every particle transition passes through). If `pi.connected` is `False` (now only possible after the narrow #2–#5 trigger), it calls `close_all_shutters()` immediately, sets `is_paused = True` and `mode_printing = "none"` (deliberately *not* touching `i_global`, `node_results`, or any drift-history list, so progress survives the pause automatically since nothing resets those attributes), and emits the new `stageDisconnectedSignal(str)` → `Frontend._show_stage_disconnected_dialog()`, a modal with a dedicated "🔌 Reconectar y Reanudar" button wired to the new `Backend.resume_after_reconnect()` slot, which calls `pi.connect()` and — on success — re-enters `_grid_move()` at the still-pending `i_global` node. On failure, re-emits the same signal with an updated message so the operator can retry.
  8. **Unrelated latent bug fixed while editing the same import line**: `modules/measurements.py` used `close_all_shutters()` at line ~1697 (`reset_all()`) but never imported it — would have raised `NameError` the first time that path executed. Found and fixed via the new automated test for #7 (which reproduced it immediately).
  9. **Test infrastructure**: `tests/test_nanopositioning_regimes.py` — unlike every other file in `tests/`, had no `sys.path.insert(BASE_DIR)` boilerplate at all, so it only worked under `pytest` because `tests/conftest.py` (added in `DEC-010`) does that for it. Run directly as `conda run -n printing3 python tests/test_nanopositioning_regimes.py` (the exact command specified for this mission, in a **different** conda env — Python 3.12 — than the rest of this session's testing, which uses the anaconda base env's Python 3.13), it failed with `ModuleNotFoundError: No module named 'config'`. Fixed with the same standard boilerplate (`sys.path.insert` + `import config` before `PyQt6`) used everywhere else in `tests/`.
  10. **New test file**: `tests/test_pi_stage_resilience.py` (18 tests) — `_MockPI.MOV()` clamping (3 tests); `_PIController` exercised directly via an injected `_FakeGCSDevice` test double (`pipython` isn't installed in this environment, so `_PIController.__init__` always leaves `self._dev = None` — the fake is attached manually post-construction to reach the real code paths): `GCSError` isolation on `MOV`/`qPOS`/`qONT` (3 tests), retry-then-succeed and retry-then-degrade-without-disconnecting on `qPOS`/`qONT` (3 tests), generic-communication-error disconnect only after a failed auto-reconnect vs. recovery via a successful one (2 tests), `is_physically_connected()` not calling `qIDN()` (1 test); grid pre-flight range check — out-of-range blocked before movement, in-range proceeds, drift-margin-triggered block (3 tests); disconnect-pauses-and-preserves-index and resume-from-pending-node (2 tests).
* **Outcome**: **ACCEPTED**. `conda run -n printing3 python tests/test_nanopositioning_regimes.py` (exact command requested): 6/6 `OK`. `tests/test_pi_stage_resilience.py`: 18/18 passing. `pytest tests/` full collection: **204 passed** (up from 186 in `DEC-010`), same 2 pre-existing unrelated failures as `DEC-010` (confirmed via two consecutive full-suite runs). `graphify update .` run cleanly post-edit.

### DEC-012: Universal Template Matching and Crystallographic Generalization for Hexagonal and Honeycomb Lattices in 2D Disorder Analyzer
* **Context**: `core/lattice_disorder.py`/`analysis/lattice_disorder_gui.py` (Fase 1) only supported square/rectangular Bravais lattices, analyzed via axis-aligned integer-index KDTree matching (`analyze_real_space_kdtree`). The lab needed to characterize two additional high-priority nanolithographic geometries: hexagonal/triangular monoatomic lattices ($\gamma=60°$, $Z=6$) and honeycomb/graphene lattices (2-atom basis, subrefs A/B, bond coordination $Z=3$), neither of which fits an axis-aligned integer-index scheme. The mission asked for a universal engine that (a) consumes `core/lattice_generator.py`'s already-validated `LatticeLayer`/`BasisAtom`/`BoundingGeometry`/`CrystalGridComposer` classes as the ideal-template source instead of reimplementing lattice generation, and (b) performs rigid registration (translation + rotation) + KDTree matching with sublattice decoupling for the analysis side, which `lattice_generator.py` does not provide (it only generates ideal templates, never registers them against noisy experimental data).
* **Decision**:
  1. **`generate_ideal_lattice_template(lattice_type, a, b, gamma_deg, boundary_type, boundary_size_nm, rotation_deg, ...)`**: thin bridge to `core.lattice_generator.CrystalGridComposer` — builds a `LatticeLayer` (µm-converted `a`/`b`, `gamma_deg`=60° auto for hex families, atoms from `BasisAtom`), delegates cell expansion/rotation/boundary-clipping to the existing composer, converts nodes back to nm, and labels each node with `sublattice_id` from `material_id`.
  2. **Honeycomb basis bug found and worked around, not fixed at the source**: numerically verified that `LatticeLayer._default_basis_for_type()`'s honeycomb/graphene basis ($u=1/3, v=2/3$), combined with the $\gamma=60°$ convention `grid_generator.py` applies to hexagonal families, does **not** produce a valid honeycomb (4 distinct bond distances in the first shell — $0.2a, 0.4a, 0.529a \times 2$ — instead of 3 equidistant neighbors at $a/\sqrt{3}$). The correct fractional basis for this $\mathbf{a}_1,\mathbf{a}_2$ convention is $u=1/3, v=1/3$ (verified: exactly 3 neighbors at $a/\sqrt{3}$, then 3 at $a$). `generate_ideal_lattice_template()` uses the corrected basis locally via an explicit `atoms=[...]` override; `core/lattice_generator.py` itself is **not modified** (out of scope — it also drives real sample fabrication via `grid_generator.py`, so any honeycomb sample already printed with the stock basis should be re-checked against $a/\sqrt{3}$; recommended as a follow-up task).
  3. **`register_and_match_template(x_real, y_real, template_x, template_y, template_sublattice, ...)`**: rigid registration via centroid alignment + a 2-stage (coarse exhaustive sweep → fine refinement) rotation search minimizing mean-squared nearest-neighbor residual — deliberately **not** a local-gradient optimizer, per the mission's own falsification concern that a naive optimizer can lock onto a neighbor-spacing-periodic local minimum instead of the true mounting misalignment ($1$–$5°$ tolerance is realistic). Followed by bounded KDTree matching (`max_dist_nm`, default half the template's median nearest-neighbor distance) with per-particle sublattice labeling and **independent vacancy accounting per subred** (`n_vacancies_by_sublattice`).
  4. **`compute_voronoi_topology(..., ideal_z=4)`** generalized with an `ideal_z` parameter (default 4, backward-compatible) so `defects_mask = coordination != ideal_z` works for `ideal_z=6` (hexagonal) and `ideal_z=3` (honeycomb — verified numerically that a honeycomb lattice's *geometric* Voronoi coordination, dominated by the 3 much-closer bond neighbors, really is 3, not 6; this coincides with but is computed independently from the bond coordination).
  5. **Real bug found and fixed in the Fase-1 vertex-merge heuristic (`_merge_close_polygon_vertices_xy`)**: the original sequential seed-at-`vertices[0]` merge algorithm silently failed whenever a degenerate vertex cluster of 3+ points straddled the polygon array's wrap-around boundary (index `k-1 → 0`) — common in honeycomb cells (3-vertex triangles, higher per-vertex sensitivity to positional noise than 4+-constrained square cells) but rare enough in square lattices to have gone undetected in Fase 1's own regression test. Measured: only ~50-58% of honeycomb cells correctly resolved to $Z=3$ under 3% bond-length noise. Replaced with a topologically robust cycle-graph formulation (count "far" — i.e. non-degenerate — edges around the vertex cycle; immune to where the array happens to start/end) plus raising the merge tolerance from 10% to 20% of the median nearest-neighbor distance (triangular honeycomb cells have less geometric redundancy than square cells' 4+ constraints, so the same absolute positional noise displaces a Voronoi vertex proportionally more) — verified this doesn't hide the pre-existing real-vacancy-defect regression test. Result: honeycomb $Z=3$ detection rose to 96%+ under the same noise level.
  6. **`compute_bond_orientational_order(..., n_fold=None)`** generalized to optionally compute a generic $\psi_n$ (e.g. `n_fold=3` for honeycomb's native 3-fold bond symmetry) alongside the existing $\psi_4$/$\psi_6$, returned under `psi_n_local`/`psi_n_mean`.
  7. **`compute_basis_structure_factor(Gx, Gy, sublattice_offsets_nm)`**: analytic $|F(\mathbf{G})|^2 = |\sum_\kappa e^{-i\mathbf{G}\cdot\mathbf{d}_\kappa}|^2$ for an $N$-atom basis (honeycomb: $F=1+e^{-i\mathbf{G}\cdot\boldsymbol{\tau}}$), provided as an independent geometric prediction; the effect itself already emerges naturally from the existing NUFFT ($S(f_x,f_y)$ computed directly over both sublattices' real positions), no multiplicative term needed there.
  8. **`extract_angular_profile(S, fx, fy, angle_deg, band_width_bins)`** and **`compute_radial_azimuthal_profile(S, fx, fy, n_r_bins)`** (the latter fully vectorized via `np.bincount`, no per-angle Python loop): generalize the existing fixed-45°-diagonal cartesian cut extraction to arbitrary angle and to full azimuthal integration $S(q)$, needed for hexagonal 0°/60°/120°-family symmetry cuts.
  9. **Real bug found and fixed — hexagonal reciprocal-lattice orientation**: `run_hexagonal_monte_carlo_calibration`'s first draft assumed the 6 equivalent first-order Bragg directions sit at $0°,60°,120°,\ldots$ (aligned with the real-space lattice vectors). Numerically brute-forced the true Bragg peak location against `generate_ideal_lattice_template`'s actual output and found it at $-30°$ — the reciprocal lattice of a triangular Bravais lattice is rotated 30° relative to the real one (standard crystallographic result, but not assumed correctly on the first attempt). The wrong angles produced a **monotonically increasing** (physically backwards) Debye-Waller attenuation curve with $\sigma$; corrected to $-30°,30°,90°,150°,210°,270°$, now monotonically decreasing with $R^2>0.998$. Same fix applied to the Pestaña 3 (reciprocal space) hexagonal Bragg-marker overlay and radial-profile ring in the GUI.
  10. **`run_hexagonal_monte_carlo_calibration(lattice_type, a, boundary_type, boundary_size_nm, ...)`**: new, separate from (not bolted onto) `run_monte_carlo_calibration` — reuses `generate_ideal_lattice_template` for the bounded ideal grid, injects vacancies/noise with the same statistical scheme as Fase 1, evaluates $S$ at $f_0=2/(\sqrt{3}a)$ averaged over the 6 corrected Bragg directions via a single vectorized `np.outer` + complex-sum BLAS-friendly evaluation (no Python loop over direction/frequency), fits via the existing (unmodified) `fit_debye_waller_curve`.
  11. **Real crash fixed in a pre-existing Fase-1 function, exposed (not caused) by hexagonal data**: `compute_analytical_bragg_relations` raised `ZeroDivisionError` (`1.0/aniso_ratio` unguarded, only the denominator of `aniso_ratio` itself was guarded) whenever `sigma_wilson_x` degenerated to exactly 0 — which happens routinely when cartesian Wilson-plot analysis (designed for square/rectangular peaks on the fx/fy axes) is run against hexagonal data whose true Bragg peaks aren't on those axes. Fixed with a second guard (`max(1e-6, aniso_ratio)`); `_on_recalculate_reciprocal()` in the GUI now also fully replaces the cartesian-specific metrics text (anisotropy/Hx/Hy/Wilson jargon, meaningless for this geometry) with hexagon-appropriate text (detected ring radius vs. $f_0$, correct Bragg directions) instead of leaving the misleading cartesian numbers on screen.
  12. **GUI (`analysis/lattice_disorder_gui.py`)**: Pestaña 2 gained a "Tipo de Red y Geometría Envolvente" group (lattice-type/boundary-type/boundary-size/auto-rotate/manual-rotation controls, shown only for hex families) that dispatches `_on_recalc_grid()` between the Fase 1 rectangular KDTree path and the new template-matching path via an adapter (`_convert_template_match_to_kdtree_dict`) that maps `register_and_match_template`'s output onto the exact dict schema the rest of the pipeline (topology plots, histograms, metrics table) already consumes — no downstream rendering code needed forking. Subred A/B rendered in distinct colors on the topology canvas. Pestaña 3's Bragg-marker overlay and Pestaña 4's Monte Carlo worker (`MonteCarloWorker.hex_params`) both dispatch on the same lattice-type selector.
  13. **Real bug found and fixed — sub-nanometer template pre-centering silently corrupting the boundary clip**: the GUI's first draft called `generate_ideal_lattice_template(..., center_x_nm=mean(x_real), center_y_nm=mean(y_real))` before registration, intending to pre-center the template near the real data. `BoundingGeometry.is_inside()` (core/lattice_generator.py) evaluates the envelope shape fixed at the origin, while `offset_x`/`offset_y` shift the *lattice points* before that check — so a real-data centroid of even ~1 nm (utterly negligible vs. a 500 nm period) could flip an entire lattice row exactly tangent to the hexagon's flat edge from inside to outside, silently producing a structurally asymmetric template (confirmed numerically: node count 217→192, actual centroid displaced by *287 nm* in Y for a *0.86 nm* requested shift) and inflating `sigma_pos` ~10x (12 nm → 120 nm) after registration. Root cause: this pre-centering step was entirely redundant with `register_and_match_template`'s own internal centroid alignment, and actively harmful. Fixed by removing the pre-centering call from the GUI entirely (always generate the template at the origin for this workflow); documented as a known sharp edge in `generate_ideal_lattice_template`'s docstring for any future caller of `center_x_nm`/`center_y_nm`, with the underlying `core/lattice_generator.py` boundary-recentring gap itself left unfixed (out of scope, same rationale as the honeycomb-basis finding in #2).
* **Outcome**: **ACCEPTED**. 4 genuine numerical/physical bugs found and fixed during development via direct numerical verification (not assumed correct from formulas alone): the honeycomb basis geometry, the Fase-1 Voronoi vertex-merge wrap-around gap, the hexagonal Bragg-direction assumption, and the sub-nanometer template pre-centering boundary-clip corruption — each confirmed by brute-force cross-check against `generate_ideal_lattice_template`'s actual output before being written into the codebase as corrected constants/patterns, and each covered by a dedicated regression test (`test_generate_ideal_lattice_template_honeycomb`, `test_register_and_match_template_honeycomb_sublattices_and_vacancies`, `test_run_hexagonal_monte_carlo_calibration_monotonic_attenuation`, `test_register_and_match_template_hexagonal_robust_to_unshifted_template` + `test_generate_ideal_lattice_template_center_offset_sensitivity_documented`). One additional pre-existing Fase-1 crash bug (`ZeroDivisionError` in `compute_analytical_bragg_relations`) fixed with a regression test (`test_analyze_reciprocal_space_2d_no_crash_on_hexagonal_data`). `pytest tests/test_lattice_disorder.py`: 53/53 passing (13 new Fase-2 tests). `pytest tests/`: 227 passing, same 2 pre-existing unrelated failures. `graphify update .` run cleanly post-edit. Known, documented (not fixed in this phase) limitation: rigid registration assumes a single global rotation, not local non-rigid deformation (lens field curvature etc.) — `compute_quiver_and_strain`'s global affine tensor is offered as a complementary diagnostic, itself also global-only.

### DEC-013: Multi-Agent Hardware-Safety Audit Remediation (Lotes 1-6)
* **Context**: A 4-agent audit panel (`instrumentation` ×2, `software-architect`, `experimentalist`) reviewed every module that commands physical hardware (PI E-517 stage, laser shutters, AOTF power, EMCCD/Shamrock) for the 6 anomaly patterns in `CLAUDE.md`'s audit taxonomy (indeterminate timing, missing settling time, GUI flooding, orphaned commands, USB/bus collisions, coordinate mapping bugs). A `devil-advocate` verification pass adversarially re-checked the 8 P0 claims that came back, confirming 7/8 (1 — `ANOM-FOCUS-01` — downgraded from "always leaves the shutter open" to "real for all 3 routines but only genuinely unbounded-open in `_focus_autocorr_lin`"). The user staged remediation into 6 explicit batches ("Lotes"), ordered by ascending physical blast-radius and by shared-file/shared-thread dependency, and **deliberately excluded** several other confirmed findings from this round for a separate pass: `ANOM-NIDAQ-01/02` (shutter/flipper state set optimistically before the hardware write is confirmed), `ANOM-MEASURE-01` ("Next Index" button moves the stage without closing the shutter), `ANOM-CONFOCAL-01` (Stop button can miss the active scan timer if the mode combo changed mid-scan), `ANOM-NIDAQ-03/04` (missing locks on `set_laser532_voltage`/`close_all_tasks`), `ANOM-XTHREAD-01` (silent trace-acquisition gap while focus.py blocks the shared confocal thread), `ANOM-MEASURE-03/04` (blind-sleep settling in most of `measurements.py`'s reposition moves), `ANOM-CONTRAPROP-01b` (Auto-CM checkbox no-op), and `ANOM-SYS201-01` (documented "500ms heartbeat" vs. actual 30s/100ms-poll watchdog design, and the unguarded "Sin límite" escape hatch).
* **Decision**:
  1. **Lote 1 — `modules/focus.py` (ANOM-FOCUS-02 → 03 → 01, in dependency order)**: new `Backend._clamped_zo(center_z)` clamps the Z-ramp trigger geometry (used by `WOS`/`CTO`/`WAV_LIN`, which go straight to the raw GCS driver with no clamping of their own) to the same `[0, PI_STAGE_RANGE_UM]` range `pi.MOV()` already clamps at the driver level — previously `self.zo` could diverge from the physically-reachable position near either Z travel limit. `_move_z()` gained `timeout_s=10.0`, raising `TimeoutError` instead of polling `qONT()` forever on a genuine servo/piezo stall (confirmed reachable: only `qONT()` *read exceptions* fail safe in `config.py`, a legitimate not-on-target *value* has no other exit). New `MAX_RAMP_RETRIES=5` class constant and `_ramp_lin_with_retry_cap()` cap the previously-unbounded `while flag:` retry loops in all three Z-ramp routines (`focus_go_to_maximum`, `focus_lock_lin`, `focus_autocorr_lin_x2`/`_focus_autocorr_lin`) when `_z_profiles()` repeatedly fails to detect valid trigger edges — each site now guarantees the shutter closes (`try/finally`) even on abort, and `focus_autocorr_lin_x2` always emits `autofinishSignal(mode_printing)` regardless of success, since `app.py`/`contrapropagante.py` wire that signal to `MeasBackend.grid_finish_autofoco()` — without it, a stuck autofocus during automated printing would hang the entire print job waiting for a callback that never fires. Fold (P3): the three floating plot windows (`plot_focus`/`plot_lock`/`plot_auto`) now close the previous window before opening a new one instead of accumulating orphans. **Blind spot closed**: `_MockNITask` always synthesizes a clean trigger, so none of this retry-cap code was exercised by any existing SAFE_MODE test before this fix — `tests/test_focus.py`'s fault-injection tests (monkeypatching `Backend._ramp_lin` to always report "not found") are the first coverage of this failure class.
  2. **Lote 2 — `modules/trace.py` + `core/nidaq.py` (ANOM-TRACE-01, fold ANOM-TRACE-02)**: `channels_photodiodos()` gained `continuous: bool = False` — the default preserves the exact `AcquisitionType.FINITE` behavior `focus.py`'s trigger-synchronized single-shot Z-ramp read requires (touched one commit earlier in Lote 1; changing the shared function's default would have silently broken it), while `continuous=True` configures `AcquisitionType.CONTINUOUS` with an enlarged buffer for a Task meant to be created once and read repeatedly. Both acquisition loops in `trace.py` (`_start`/`_trace_update`/`_stop_and_save` for the main trace, `set_bs_only_active`/`_bs_only_update`/`play_pause` for the BS-only mode) now create their Task once per session instead of once per 35ms tick — the previous per-tick create/destroy overhead made the true inter-sample cadence scheduler/driver-dependent, undermining the "10 kHz"-labeled acquisition rate. Fold ANOM-TRACE-02: emitted payloads capped to `SEND_WINDOW=2000` samples in both loops; full history stays intact in `self.timeaxis`/`self.intensity_*`/`self.bs_*` for `save_trace()`/`save_bs_trace()`. **Bug found and fixed in the same pass**: capping the emitted arrays broke `Frontend.get_data()`, which sliced the last `SHOW=1000` samples using `n` (`data[0]`, the unbounded global sample counter) instead of the actual received array length — once `n` exceeds `SEND_WINDOW`, the two stop matching and the old slice returned empty/wrong data. Fixed to slice by `len(timeaxis)` instead; caught by a dedicated regression test (`test_frontend_get_data_uses_received_array_length_not_global_counter`) before it could reach a real session.
  3. **Lote 3 — `modules/measurements.py` (ANOM-MEASURE-02)**: `grid_trace_detect()` — the per-sample callback that fires continuously while the print-cycle shutter is open — now renews the watchdog via bare `heartbeat_shutter()` (no argument, honoring the global auto-close policy rather than hardcoding a value) on every sample. Previously nothing in the printing/trace loop renewed the watchdog at all; with `DEFAULT_PRINTING_TMAX=20.0` and Healing Pass adding `+10.0`, a retried node's effective exposure window (`30.0s`) landed exactly on the watchdog's own 30s default deadline, so the shutter could be force-closed mid-pulse without the print state machine's knowledge (it would just log the node as `"timeout"`, indistinguishable from a real printing failure).
  4. **Lote 4 — `modules/confocal.py` + `contrapropagante.py` (ANOM-CONFOCAL-02 → 03 → 04/CONTRAPROP-01a)**: found `modules/confocal.py` already carried an **uncommitted, in-progress diff** from earlier in the session attempting exactly the ANOM-CONFOCAL-03 fix — built on top of it rather than discarding it (its `self.i % 10 == 0` GUI-emit throttling and per-pixel 3ms settle were kept), but its flyback-settle placement had the identical bug the audit flagged (the 35ms pause sat in the row-end branch, *before* the flyback `pi.MOV()` — which only executes on the *next* tick). All 7 `heartbeat_shutter(30.0)` call sites (6 in `confocal.py`: `_measure_4_corners_tilt`, `_scan_step_xy`, all 4 `_scan_ramp_*`; 1 in `contrapropagante.py`: `ConfocalDualBackend._scan_ramp_xy`) now call bare `heartbeat_shutter()` — this literal anti-pattern (an explicit numeric argument always overrides the global policy by design, per `DEC-010`) reproduced the exact bug `DEC-010` had already fixed for callers that omit the argument entirely. `_scan_step_xy`'s long settle now applies to `self.i==0` (the pixel that follows the real flyback move) instead of the row-end branch. New `_wait_axis_settle(axes, timeout_s=0.05)` — identical implementation in both files, bounded like `_move_z` in Lote 1 — confirms `qONT()` before triggering the wave-table (`pi.WGO`) in all 4 ramp functions of `confocal.py` and the one in `contrapropagante.py`; previously the trigger could fire while the axis was still in inertial flight from the row-transition `pi.MOV()`. Fold (P3): `confocal.py::_profiles()` now prints a warning on each positional-fallback branch (no trigger edges detected) instead of silently mis-segmenting the gone/back trace.
  5. **Lote 5 — `pyspectrum/modules/hyperspectral_confocal.py` + `pyspectrum/window.py` (ANOM-HYPERSPEC-02 → 01)**: `Frontend` gained a `combo_laser` (`SHUTTERS`-populated, same wording/pattern as `growth_kinetics.py::cmb_laser`) — this module previously had **no** excitation-laser selector or shutter lifecycle at all. `startScanSignal` gained a `str` (laser) parameter; `Backend.start_scan()`/`stop_scan()` now open/close the selected laser (`stop_scan()` is the single centralized stop path already used by normal completion, the Stop button, and `hardware_session.emergencyStopSignal` — one fix covers all three), and `_scan_step()` renews the watchdog per point via bare `heartbeat_shutter()`. `ConfocalBackend` is then moved to a real `QThread` (`window.py`, `moveToThread()` — the **second** architectural exception in this codebase to the QTimer-on-GUI-thread convention, after `linescan_spectroscopy.py`/`DEC-006`): each map point previously blocked the entire GUI thread (`pi.MOV()` + a full CCD exposure/readout), freezing the whole window — including the Stop button and E-STOP — for the exposure's duration, hundreds to thousands of times per map. **Bug found and fixed in the same pass**: `window.py::closeEvent()` already called `self.confocal_backend.stop_scan()` directly — safe before the thread move (same-thread call), but a cross-thread hazard afterward (a direct Python method call touches `self.scan_timer`, now owned by `confocal_thread`, without Qt's signal-marshalling). Fixed with `QtCore.QMetaObject.invokeMethod(self.confocal_backend, "stop_scan", QtCore.Qt.ConnectionType.BlockingQueuedConnection)`. Verified the actual thread affinity directly (`confocal_backend.thread() is confocal_thread`, distinct from `QThread.currentThread()`, and `scan_timer.thread()` migrated with its parent) rather than only checking for the absence of a crash; the pyspectrum suite was run 3 consecutive times (not just once) given QThread-topology changes are inherently more prone to nondeterministic races than the rest of this remediation — stable all 3 runs.
  6. **Lote 6, independent fixes**: `modules/measurements.py::grid_change_index()` now refuses (with an operator-visible console warning) to reassign `self.i_global` while `mode_printing != "none"` — previously, typing in the "Target Index" field mid-print (a live `textChanged`-driven signal) reassigned the index immediately, so an in-flight `grid_trace_detect()` result could be attributed to the wrong node via `_save_trace()`'s `i_global`-derived filename. Removed a dead `from scipy import optimize` import in `contrapropagante.py` (zero usages).
  7. **Lote 6, PSF-mode parity in `contrapropagante.py::ConfocalDualBackend` (user-directed scope expansion, decided via explicit question)**: the PSF_mode combo (`x/y, x/z, y/x, y/z`) was more than decorative — `start_scan()` never even read `psf_mode_opt`, it unconditionally configured and ran `x/y` regardless of the operator's selection. Implemented the other 3 modes mirroring `modules/confocal.py::Backend`'s existing per-mode axis/trigger/range conventions exactly (including its inherited `y/z` quirk of storing `Ny` values into row `self.i`, which ranges over `Nx` — dormant when `Nx==Ny`, the default, and deliberately *not* "corrected" here to avoid diverging from the reference implementation's behavior): new `_configure_ramp_y()` (mirrors `_configure_ramp_x`, needs `extra_y`/`range_total_y`, now computed in `scan_ramp_parameters()`), 3 new `QTimer`s (`PDtimer_rampxz/yx/yz`), and `start_scan()` now dispatches configure-function + timer by `psf_mode_opt`. Factored the dual-channel acquisition (previously inlined only in `_scan_ramp_xy`) into shared `_acquire_dual_pixel_row()`/`_bin_or_interp()`/`_synthetic_dual_grid()`/`_finish_ramp_scan()` helpers, reused by all 4 modes including a refactored `_scan_ramp_xy` (verified numerically equivalent to its pre-refactor SAFE_MODE output — same Gaussian formula, same row slice — before treating the refactor as safe). `stop_scan()` now stops whichever of the 4 timers is active instead of only `PDtimer_rampxy`.
  8. **Lote 6, cross-process PI E-517 access — confirmed, not fixed**: the audit had flagged this as an *unconfirmed* deployment risk (the per-process `threading.RLock()` in `config.py::_PIController` provides no protection if two OS processes both talk to the same physical stage). Asked directly: PySpectrum can run concurrently with **either** the single-laser "microscopio derecho" app **or** `contrapropagante.py`, though those two never run together with each other. This makes the risk **real, not hypothetical** — two independent processes can issue unsynchronized `pi.MOV()`/`qPOS()` calls against the same USB/serial port. A cross-process exclusion mechanism (file lock or named OS mutex) is explicitly **out of scope for this round** (per the option the user selected) and is logged here as a confirmed follow-up item, not merely an "unconfirmed risk" as the original audit had it.
  9. **Lote 6, deliberately left unfixed**: `core/shutters.py::Backend.power_change()`/`notch532_change()` echo the same before-the-write optimistic-state pattern as `ANOM-NIDAQ-02` (both out of scope this round) — `up_flipper()`/`down_flipper()` return `None`, so these UI slots have no success/failure signal to condition their `flipper_state_signal.emit(...)` on; a standalone patch here without `ANOM-NIDAQ-02`'s root fix would give false confidence. `core/nidaq.py::_watchdog_active` (flagged as a "dead toggle" by the audit) is not actually a bug — it is a daemon-thread loop guard by design (the watchdog is meant to run for the process lifetime); left unchanged.
* **Outcome**: **ACCEPTED**. New test files: `tests/test_focus.py` (9), `tests/test_trace.py` (6), `tests/test_measurements.py` (5, across Lotes 3 and 6), `tests/test_confocal.py` (11, across Lotes 4 and 6), `tests/test_hyperspectral_confocal.py` (7) — 38 new tests total, each lote's regression run confirmed against the full suite before proceeding to the next (`236 → 242 → 245 → 251 → 258 → 265 passed`, tracking cumulative work). Final full run: **265 passed**, 2 pre-existing failures unrelated to this work and independently confirmed via clean-checkout `git stash` comparison before this remediation began: `test_hardware_dashboard_logic.py::test_hardware_manager_isolation` (real, reproducible logic bug in `core/hardware_manager.py`, already logged in `DEC-010`) and `test_pyspectrum_stability_safety.py::TestPySpectrumSafety::test_calibration_txt_persistence_roundtrip` (full-suite test-ordering flakiness, passes standalone, reproducible on the clean checkout with zero code changes — cross-test state pollution from an unidentified source, also already flagged in `DEC-010`). `graphify update .` run cleanly after every lote. Follow-up items explicitly deferred to a separate round: `ANOM-NIDAQ-01/02`, `ANOM-MEASURE-01`, `ANOM-CONFOCAL-01`, `ANOM-NIDAQ-03/04`, `ANOM-XTHREAD-01`, `ANOM-MEASURE-03/04`, `ANOM-CONTRAPROP-01b`, `ANOM-SYS201-01`, and the cross-process PI E-517 exclusion mechanism (item 8 above).

### DEC-014: Canon EDSDK Camera Stability Audit + `StageCameraTransform` Kinematics + Bidirectional Spatial Sync (Cámara ↔ Confocal ↔ Impresión)
* **Context**: The user requested a full overhaul of the camera live-view suite (`modules/camera.py`, `core/canon_edsdk.py`) — explicitly noting it "ha recibido muchos parches y ninguno ha logrado darle estabilidad total ni implementar las funciones de zoom de una forma cómoda y útil para el usuario, además de la propensión del programa a colgarse" — plus a formal kinematic transform between the platina (PI E-517), cámara réflex and confocal coordinate systems, and bidirectional overlay sync between the Camera, Confocal and Printing/Dimers modules. Went through the full `deliberative-implementation` protocol: Round 1 (4 parallel research agents: camera-stability adversarial audit, metrology on the platina↔cámara transform, software-architect on signal topology, qa-ux-auditor on the calibration workflow) → Round 2 (engineering blueprint) → user approval to implement in 6 phases (A–F). Explicit standing instruction from the user throughout: "no te confíes de las implementaciones previas, revisa siempre la lógica de la implementación" — every reused piece of legacy logic (the `DIRECTION_MAP` regime relationships, the zoom crop-window geometry, `gridplotSignal`'s relative-vs-absolute semantics) was independently re-derived or cross-checked against source, not assumed correct because it already shipped.
* **Decision**:
  1. **Fase A — Camera crash/hang/zoom-UX audit + `StageCameraTransform` core**: a dedicated adversarial audit (user-mandated scope addition, not in the original brief) found and fixed `ANOM-CAM-01` (a deterministic self-deadlock: `_edsdk_lock` was a non-reentrant `threading.Lock`, and `set_live_view_zoom()` called `_apply_zoom_position_from_center()` — which re-acquires the same lock — while still holding it; fixed with `threading.RLock()` + restructuring the nested call to happen outside the outer `with` block), `ANOM-CAM-02`/`02b` (both `CameraWindow.closeEvent()` and `app.py::Backend.close_all()`'s camera teardown called worker methods directly from the GUI thread despite the worker living on its own `QThread` — fixed with `QMetaObject.invokeMethod(..., BlockingQueuedConnection)`), `ANOM-CAM-04` (`kEdsCameraStatusCommand_UILock`/`UIUnLock` used but never defined — silent `NameError` swallowed by a bare `except Exception: pass` in the NonAF photo fallback; added with values verified against `EDSDKTypes.h:468-469`), and `ANOM-CAM-06` (`OverlayWidget.set_zoom_level()` was defined twice in the same class — Python silently used only the second; the dead first definition, lacking the PiP-freeze logic, likely explains why prior zoom patches never fully took effect — removed). Independently found (outside the camera audit's scope, per the "no te confíes" mandate): `core/nanopositioning.py::Backend.move()`/`_moveto()` polled `pi.qONT()` with no timeout — same unbounded-poll pattern as `ANOM-FOCUS-03` (`DEC-013`) — fixed with `timeout_s` parameters. New `core/stage_camera_transform.py::StageCameraTransform`: calibrated in *physical PI stage axes* (1, 2 — the same axes `pi.MOV()`/`pi.qPOS()` use natively, not per-regime labels) and in *full sensor pixels* (4752×3168, zoom-invariant by construction) rather than a separate matrix per coordinate regime or per zoom level — justified by `SYS-103`'s own documented "Invariance Principle" (regime is a pure UI relabeling that never changes actuator kinematics, confirmed by `tests/test_coordinate_nomenclature_sync.py`). Confirmed by direct hand-derivation (not assumed) that the transform is a *reflection* (`det(M) < 0`), not a rotation, matching the Round 1 metrology finding; `KNOWN_SIGN_PATTERN` encodes the user's physically-verified convention (platina arriba → láser +u; platina derecha → láser +v) purely as a post-calibration sanity check, never as a default/assumed value.
  2. **Fase B — Calibration wizard + fiducial cross-validation**: per the user's explicit choice ("me gusta la confirmación visual humana obligatoria, pero me gustaría corroborarla con dos partículas impresas a una distancia conocida y medida por microscopía confocal"), implemented *both* a step-based two-axis `StageCalibrationWizardDialog` (moves the stage a known physical step per axis, captures before/after frames, requires mandatory human visual confirmation of the detected feature before accepting each axis) *and* a separate `FiducialValidationDialog` for corroboration against a printed dimer pair at a known, confocal-measured separation — explicitly a cross-validation of an already-accepted calibration, not a second independent calibration method (a single dimer pair gives only one equation/direction, insufficient to derive a full 2×2 matrix alone). New `core/stage_camera_calibration.py`: `display_fraction_to_sensor_px` (1x-only), `detect_dominant_feature` (trackpy-based), `StepCalibrationSession`, `compare_fiducial_calibration`. **Bug found and fixed before it reached a test run**: `_open_fiducial_validation()` initially copied `_open_set_scale()`'s ROI-crop pattern, which would have silently corrupted the sensor-coordinate conversion (`FiducialValidationDialog` needs fractions relative to the *full* 1x frame) — fixed to always emit the uncropped frame. **Bug found via a genuinely hung test run** (isolated with `timeout 15`, one test at a time): `FiducialValidationDialog._on_record()`'s real `QMessageBox.information()` blocks forever under `QT_QPA_PLATFORM=offscreen` with no automatic dismissal — fixed by monkeypatching it in the two affected tests, a pitfall to watch for in any future dialog test.
  3. **Fase C — ROI → Confocal, rewritten on `StageCameraTransform`**: replaced the prior ad-hoc formula (a single isotropic `_um_per_px`, no zoom scaling, a hardcoded axis swap independent of the active coordinate regime — the original Round 1 finding) with the calibrated transform, mapping all 4 ROI corners (not just one diagonal) to get the correct bounding box even under slight anisotropy/shear. Per the user's explicit choice ("Confirmación explícita separada"), `_send_roi_to_confocal()` only positions the stage and loads the range/pixel parameters into the Confocal panel (`Frontend.set_ramp_parameters_from_external()`, forcing Ramp mode) — it never starts the scan; the operator must press "Iniciar Escaneo" manually. **Bug fixed**: `roiToConfocalSignal` previously never carried the computed target center (only range/pixels) and was completely unconnected anywhere in the repo — extended to a 6-argument signal carrying the center too.
  4. **Fase D — Confocal → Cámara / Impresión → Cámara overlays (cached, zoom-aware)**: new `sensor_px_to_display_fraction()` generalizes the ROI/calibration tooling's 1x-only projection to any Canon hardware zoom level, replicating (and separately verified against) `core/canon_edsdk.py::_apply_zoom_position_from_center()`'s crop-window geometry. Per the Round 1 software-architect finding, all overlay geometry is recomputed *only* on the relevant upstream signal (`nanoWorker.read_pos_signal`, `confocalWidget.parametersrampSignal`/`parametersstepSignal`, `printingWorker`/`dimersWorker`'s `referenceSignal`/`gridplotSignal`, and the 4 sites where Canon hardware zoom/pan state mutates) — never inside the 25 fps frame-update path. Confocal scan-area box (4 corners) draws all-or-nothing if any corner falls outside the current view (a partial quadrilateral would confuse the operator); the printing grid drops individual out-of-view points silently and draws whichever remain (expected behavior when panning across a large grid). Both are independently toggleable via new toolbar buttons. **Two bugs found by the phase's own test suite, not by inspection**: (a) `update_printing_reference()`'s `except (TypeError, ValueError)` never caught `measurements.py`'s `['NaN','NaN','NaN']` reference-cleared sentinel, because `float('NaN')` is a *valid* Python conversion that returns `nan` rather than raising — fixed with an explicit `math.isnan()` check; (b) "Limpiar Todo" cleared `_ref_set` (invalidating the anchor both overlays are computed from) but left their cached screen-space points stale until the next upstream signal arrived — fixed to also clear both caches there.
  5. **Fase E — EDSDK live-view stream size**: per the user's explicit choice ("Solo el fix seguro: tamaño fijo en vez de 0"), `get_live_view_frame()`'s `EdsCreateMemoryStream(0, ...)` — re-allocating from zero on every one of up to 25 frames/second — was changed to a fixed initial size. The value (2 MiB) was not picked arbitrarily: it is the exact constant Canon's own official EDSDK sample (`Sample/CSharp/.../DownloadEvfCommand.cs:41`) uses for this identical call, and `EDSDK.h`'s own documentation confirms the stream still auto-extends if a real frame exceeds it ("In the case of writing in excess of the allocated buffer size, the memory is automatically extended") — so the fix introduces no hard frame-size ceiling, it only removes the repeated zero-to-N reallocation churn that was a plausible contributor to the reported instability. The alternative (reusing a single stream handle across frames) was deliberately rejected as unverified SDK behavior, per the user's Round 1 choice.
  6. **Fase F — Documentation**: this entry, plus `MOD-04_Camara_Live_View_Canon_EDSDK.md` and `docs/MANUAL_USUARIO.md` §7 updated with the `StageCameraTransform` kinematics, the calibration/fiducial workflow, the rewritten ROI→Confocal flow, and the new cached bidirectional overlays.
* **Outcome**: **ACCEPTED**. New test files: `tests/test_canon_camera_stability.py` (9, including the Fase E stream-size fix), `tests/test_stage_camera_transform.py` (15), `tests/test_stage_camera_calibration.py` (13), `tests/test_stage_calibration_wizard_dialog.py` (8), `tests/test_fiducial_validation_dialog.py` (8), `tests/test_roi_to_confocal.py` (10), `tests/test_camera_overlays_projection.py` (17) — 80 new tests total, each phase's regression run confirmed against the full suite before proceeding to the next. Final full run: **345 passed**, the same 2 pre-existing failures already logged in `DEC-010`/`DEC-013` and independently re-confirmed unrelated to this work (`test_hardware_dashboard_logic.py::test_hardware_manager_isolation`, `test_pyspectrum_stability_safety.py::TestPySpectrumSafety::test_calibration_txt_persistence_roundtrip`). `graphify update .` run cleanly after every phase.

### DEC-002: Dedicated 500 ms Autonomous Watchdog Heartbeat
* **Context**: A Python crash during high-power laser irradiation could leave physical shutters open, destroying samples and objectives.
* **Decision**: Implement an autonomous background thread issuing 500 ms heartbeats to the DAQmx digital line. If the heartbeat expires without renewal, hardware auto-trips to closed state.
* **Outcome**: Zero sample damage even during unhandled Python runtime terminations.

### DEC-005: Event Isolation via `_results_stale` Flag in Lattice Disorder Analyzer
* **Context**: An internal audit (computational-physicist, metrology, qa-ux-auditor) found that `_update_real_space_analysis()` in `analysis/lattice_disorder_gui.py` silently cascaded Bloque 5 (cluster detection), Bloque 6 (grid/vacancy fit) and Fourier reciprocal-space recalculation from 12+ unrelated call sites (particle deletion, undo/revert curation, ROI apply, image invert, and — most critically — `_on_load_image()`, which could recompute clusters/grid/g(r) against a newly loaded image while still holding `locs_df` positions from the previous image). This violated the GUM traceability principle that every reported quantity must be attributable to a single, frozen input state, and contradicted the explicitly requested strict sequential flow (each block fires only from its own button).
* **Decision**: Replace the silent cascade with a `self._results_stale: bool` flag plus a non-blocking amber badge (`lbl_stale_badge`) shown in Pestaña 1. Any mutation of `locs_df` (curation, ROI, image reload) now only re-renders the particle scatter and marks results stale — it never recomputes Bloque 5/6/Fourier automatically. `_on_recalc_grid()` and `_on_recalculate_reciprocal()` remain the sole entry points for those computations and clear the flag on execution. `_on_load_image()` additionally hard-resets `locs_df`/`locs_df_raw`/`cluster_results`/`kdtree_results`/`rdf_results`/`reciprocal_results` to structurally prevent cross-image computation. A companion `unmarked_indices` set + red-square (🟥) scatter layer + "Desenmarcar" button was added so multi-Gaussian-resolved particles remain visually flagged for review until the user explicitly dismisses them.
* **Outcome**: Bloque 5, Bloque 6 and Fourier are now strictly confined to their own buttons; every reported metrological quantity is traceable to an explicit, user-triggered recompute. Verified via `pytest tests/` (33/33 lattice_disorder/export tests passing, no regressions in unrelated pre-existing failures).
